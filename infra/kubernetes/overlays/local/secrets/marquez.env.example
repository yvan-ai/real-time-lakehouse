# Marquez database credentials (dedicated database in the shared Postgres).
# Copy to marquez.env (gitignored) — bootstrap.sh generates one automatically.
MARQUEZ_DB_USER=marquez
MARQUEZ_DB_PASSWORD=change-me
