apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- batch-job.yaml

# CD pipeline updates this tag via `kustomize edit set image`.
# For a locally built image: kustomize edit set image spark-batch=spark-batch:local
images:
- name: spark-batch
  newName: ghcr.io/yvan-ai/real-time-lakehouse/spark-batch
  newTag: sha-de7e2fa
