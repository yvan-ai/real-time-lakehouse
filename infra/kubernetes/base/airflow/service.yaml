---
apiVersion: v1
kind: Service
metadata:
  name: airflow
  namespace: orchestration
  labels:
    app: airflow
    app.kubernetes.io/part-of: real-time-lakehouse
spec:
  selector:
    app: airflow
  ports:
    - name: web
      port: 8080
      targetPort: web
