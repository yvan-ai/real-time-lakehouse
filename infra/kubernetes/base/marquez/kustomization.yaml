apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# db-init-job.yaml excluded on purpose: Jobs are immutable, bootstrap.sh
# deletes and re-applies it (idempotent role/database creation).
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
