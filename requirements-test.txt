# Test-only dependencies — installed by CI (see .github/workflows/ci.yml)
# and by `make setup`.
pyspark==3.5.3
pytest>=8.0,<9
pytest-cov>=5.0
# Exchange-rates loader transform tests (pipelines/ingestion, ADR-0011)
polars>=1.0,<2
